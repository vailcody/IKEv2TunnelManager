# Contributing to IKEv2 VPN Chain Manager

Спасибо за интерес к проекту! 🎉

## Как внести вклад

### Сообщить о баге

1. Проверьте, что баг еще не был зарегистрирован в [Issues](https://github.com/vailcody/IKEv2TunnelManager/issues)
2. Создайте новый Issue с подробным описанием:
   - Версия приложения
   - Операционная система
   - Шаги для воспроизведения
   - Ожидаемое поведение
   - Фактическое поведение

### Предложить улучшение

1. Откройте Issue с тегом `enhancement`
2. Опишите предлагаемую функциональность
3. Объясните, почему это будет полезно

### Pull Requests

1. Fork репозитория
2. Создайте feature branch (`git checkout -b feature/amazing-feature`)
3. Commit изменения (`git commit -m 'Add amazing feature'`)
4. Push в branch (`git push origin feature/amazing-feature`)
5. Откройте Pull Request

## Разработка

### Требования

- Go 1.21+
- Для Linux: пакеты для сборки Fyne
  ```bash
  sudo apt-get install libgl1-mesa-dev xorg-dev
  ```

### Локальная сборка

```bash
# Клонирование
git clone https://github.com/vailcody/IKEv2TunnelManager
cd ip-ikev2-tunnel

# Установка зависимостей
go mod download

# Сборка
go build -o vpnmanager ./cmd/vpnmanager

# Запуск
./vpnmanager
```

### Запуск тестов

```bash
go test -v ./...
```

### Проверка кода

```bash
go vet ./...
go fmt ./...
```

## Стиль кода

- Следуйте [Effective Go](https://golang.org/doc/effective_go)
- Используйте `gofmt` для форматирования
- Добавляйте комментарии к экспортируемым функциям

## Версионирование

Проект использует [Semantic Versioning](https://semver.org/):
- `MAJOR.MINOR.PATCH`
- Пример: `v1.2.3`

## Лицензия

Отправляя Pull Request, вы соглашаетесь, что ваш код будет распространяться под MIT лицензией.
